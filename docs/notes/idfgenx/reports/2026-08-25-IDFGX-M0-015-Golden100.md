# IDFGX-M0-015：100 Golden 扩展报告

日期：2026-08-25  
任务：IDFGX-M0-015  
状态：done

## 实际完成

- 保留既有 20 项 Golden，新增 80 项，合计 100 项。
- 样本严格平衡：`single` 50 项，`perimeter_core` 50 项。
- 每项冻结 `spec.json`、canonical epJSON SHA-256、Zone/Surface/Window 计数；未引入集中 manifest、IDF 或运行时产物。
- 每项均经独占工作目录执行真实 EnergyPlus v23.1 设计日 V0–V6 验证。

## 验证证据

| 命令 | 结果 |
| --- | --- |
| `python -m unittest tests.integration.test_compiler_goldens -v` | 2/2 通过，100/100 Golden 通过，62.236 秒 |
| `python -m unittest tests.integration.test_compiler_goldens.CompilerGoldenTests.test_discovers_one_hundred_balanced_golden_fixtures -v` | 1/1 通过；发现 100 项，50/50 平衡 |
| `uv run python -m unittest discover -v` | 76/76 通过，65.659 秒（Pydantic 2.13.4） |
| `uv lock --check` | 通过；CPython 3.11.15，50 包 |
| `python -m compileall -q idfgenx tests` | 通过 |
| `git diff --check` | 通过 |

直接使用系统 Anaconda Python（Pydantic 2.5.3）运行全仓测试时，2 项既有 DraftValue 单测因依赖版本漂移失败；项目受管的 uv 环境使用锁定依赖后 76/76 全绿。本任务未改动相关 Schema 或 Resolver 代码。

## 未完成项与风险

- M0-015 不扩展 Compiler 支持域，未实现真实 AirLoopHVAC、PlantLoop 或设备拓扑。
- Golden 覆盖的是当前受支持的矩形 single/perimeter_core 场景；更强的输入扰动与跨平台一致性留给 M0-016/M0-017。

## 后续工作

1. M0-016：增加 round-trip、metamorphic 与 mutation 稳定性测试。
2. M0-017：对代表样本做 Windows/Linux 一致性验证。

## 相关提交

- `b6129c6 test(validation): expand compiler goldens to one hundred`
