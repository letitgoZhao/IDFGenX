# 架构决策记录（ADR）

当一个决定影响多个模块、长期兼容性、研究公平性或部署方式时，使用 ADR，而不是只写在聊天或代码注释里。ADR 保存在本目录并由任务与报告引用。

适合 ADR 的事项包括：ScenarioSpec 版本、Compiler 中间表示、EnergyPlus 版本升级、训练输出协议、数据 release 格式、任务队列和持久化方案。

命名：`ADR-NNNN-short-title.md`。状态使用 `proposed`、`accepted`、`superseded` 或 `rejected`。被替代的 ADR 不删除，链接新 ADR。
